fun main() {
    val first = readln().toInt()
    val second = readln().toInt()
    val result = first + second // производим математическое действие "суммирование двух чисел" путем получением этих
    // чисел через ввод
    println(result)
}